#include "libft.h"
#include <fcntl.h>

// void f(unsigned int i, char * p)
// {
//     *p = 'a';
// }



int main()
{
    char c[] = "walid z and z oumechtak";
    // char *p = ft_strmapi(NULL, f);
    printf("%p", ft_calloc(0,1));
}